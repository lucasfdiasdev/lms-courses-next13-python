/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'tailwindui.com'], // Add the domain where your images are hosted
  },
}

module.exports = nextConfig
